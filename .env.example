# Database configuration
# PostgreSQL connection string for the database
DATABASE_URL=postgres://your_username:your_password@localhost:5432/fssocial_dev

# JWT Secret
# Replace with a secure random string for JWT token signing
JWT_SECRET=replace-with-a-secure-random-string

# Server configuration
# Port the server will listen on (optional, defaults to 4000)
PORT=4000

# CORS configuration
# Frontend URL for CORS origin
FRONTEND_URL=http://localhost:3000

# Node environment (optional but recommended)
NODE_ENV=development